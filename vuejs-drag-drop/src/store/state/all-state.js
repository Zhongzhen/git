import { formInputList, buttonList,selectList,radio_checkboxList } from './list-form-input'
import { inputSize, buttonType } from './constant'

export const state = {
  formInputList,
  buttonList,
  selectList,
  radio_checkboxList,
  inputSize,
  buttonType
}
